# OpenSpec

本目录用于记录 RoboMind/XVLA baseline 工作的稳定契约和 active change。

- `specs/`：已经接受的长期行为契约。
- `changes/`：正在规划或实施的变更。
- `changes/archive/`：完成并归档后的变更。

当前重点：准备一条本地 SmolVLA baseline 路线，在必要时使用 RoboMIND TienKung Xsens 数据做预适配，同时保持 Isaac Sim 运行环境和训练环境相互独立。

